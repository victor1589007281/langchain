# LangChain 整体架构

## 1. 项目概述

LangChain 是一个用于构建基于大语言模型（LLM）的应用程序的强大框架。它提供了一套完整的工具和抽象，帮助开发者构建可靠的 Agent 和 LLM 驱动的应用。

### 核心特点

- **模块化设计**：组件之间相互独立，易于替换和扩展
- **模型互操作性**：轻松切换不同的 LLM 模型
- **生产就绪**：内置监控、评估和调试支持
- **丰富的集成**：支持众多模型提供商和工具
- **LCEL 表达式语言**：声明式的链式调用方式

## 2. 项目结构

LangChain 采用 **Monorepo** 架构，主要包含以下核心模块：

```mermaid
graph TB
    subgraph "**LangChain Monorepo**"

        Core["**langchain-core**<br/>核心抽象层"]
        Classic["**langchain-classic**<br/>传统功能模块"]
        V1["**langchain**<br/>主包（v1版本）"]
        CLI["**langchain-cli**<br/>命令行工具"]
        TextSplitter["**langchain-text-splitters**<br/>文本分割器"]
        StandardTests["**standard-tests**<br/>标准测试套件"]

        subgraph Partners["**集成包（Partners）**"]
            OpenAI["**langchain-openai**"]
            Anthropic["**langchain-anthropic**"]
            Chroma["**langchain-chroma**"]
            Ollama["**langchain-ollama**"]
            Others["**其他集成...**"]
        end
    end

    Core --> Classic
    Core --> V1
    Core --> Partners
    Classic --> V1
    TextSplitter --> V1

    style Core fill:#c8e6c9,stroke:#2e7d32,stroke-width:3px
    style V1 fill:#bbdefb,stroke:#1976d2,stroke-width:3px
    style Partners fill:#fff3e0,stroke:#e65100,stroke-width:2px
    style OpenAI fill:#ffe0b2,stroke:#f57c00,stroke-width:2px
    style Anthropic fill:#ffe0b2,stroke:#f57c00,stroke-width:2px
    style Chroma fill:#ffe0b2,stroke:#f57c00,stroke-width:2px
```

### 模块说明

| 模块 | 说明 | 主要功能 |
|------|------|----------|
| **langchain-core** | 核心抽象层 | 定义基础接口、Runnable、LCEL、消息类型等 |
| **langchain** | 主包（v1） | 提供高层 API，整合所有功能 |
| **langchain-classic** | 传统功能 | 包含遗留的 Chain、Memory、社区集成等 |
| **langchain-cli** | CLI 工具 | 项目脚手架、模板生成等 |
| **partners/** | 第三方集成 | OpenAI、Anthropic、Chroma 等集成包 |
| **text-splitters** | 文本分割 | 各种文本分割策略 |
| **standard-tests** | 标准测试 | 通用测试工具和标准测试套件 |

## 3. 核心架构层次

LangChain 采用分层架构，从底层到高层依次为：

```mermaid
graph TD
    subgraph appLayer["**应用层**"]
        App["**用户应用**<br/>RAG、Agent、Chatbot 等"]
    end

    subgraph orchLayer["**编排层**"]
        Chains["**Chains**<br/>链式组合"]
        Agents["**Agents**<br/>智能代理"]
        LCEL["**LCEL**<br/>表达式语言"]
    end

    subgraph compLayer["**组件层**"]
        Models["**Models**<br/>LLM/Chat/Embeddings"]
        Prompts["**Prompts**<br/>提示模板"]
        Tools["**Tools**<br/>工具集"]
        Retrievers["**Retrievers**<br/>检索器"]
        Memory["**Memory**<br/>记忆管理"]
    end

    subgraph coreLayer["**抽象层（Core）**"]
        Runnable["**Runnable**<br/>可执行抽象"]
        Messages["**Messages**<br/>消息类型"]
        BaseClasses["**Base Classes**<br/>基类定义"]
        Callbacks["**Callbacks**<br/>回调系统"]
    end

    subgraph integLayer["**集成层**"]
        OpenAIInt["**OpenAI**"]
        AnthropicInt["**Anthropic**"]
        VectorStores["**Vector Stores**"]
        OtherInt["**其他集成**"]
    end

    App --> Chains
    App --> Agents
    App --> LCEL

    Chains --> Models
    Chains --> Prompts
    Chains --> Tools
    Agents --> Models
    Agents --> Tools
    Agents --> Memory
    LCEL --> Runnable

    Models --> Runnable
    Prompts --> Runnable
    Tools --> Runnable
    Retrievers --> Runnable

    Runnable --> Messages
    Runnable --> Callbacks
    Runnable --> BaseClasses

    Models --> OpenAIInt
    Models --> AnthropicInt
    Retrievers --> VectorStores
    Tools --> OtherInt

    style appLayer fill:#e1bee7,stroke:#4a148c,stroke-width:3px
    style orchLayer fill:#c5cae9,stroke:#1a237e,stroke-width:3px
    style compLayer fill:#b2dfdb,stroke:#004d40,stroke-width:3px
    style coreLayer fill:#c8e6c9,stroke:#1b5e20,stroke-width:4px
    style integLayer fill:#ffecb3,stroke:#f57f17,stroke-width:3px
```

## 4. 核心设计理念

### 4.1 Runnable 接口

**Runnable** 是 LangChain 的核心抽象，所有组件都实现了这个接口。它提供了统一的调用方式：

```python
# 所有 Runnable 都支持这些方法
result = runnable.invoke(input)           # 同步调用
result = await runnable.ainvoke(input)    # 异步调用
results = runnable.batch([input1, input2]) # 批量处理
for chunk in runnable.stream(input):      # 流式输出
    print(chunk)
```

### 4.2 LCEL（LangChain Expression Language）

LCEL 提供声明式的组合方式，使用 `|` 操作符连接组件：

```python
chain = prompt | model | output_parser
result = chain.invoke({"input": "你好"})
```

### 4.3 回调系统

完整的回调系统支持监控、日志和调试：

```mermaid
sequenceDiagram
    participant **User** as **用户代码**
    participant **Chain** as **Chain/Runnable**
    participant **Callback** as **CallbackManager**
    participant **Handler** as **Handlers**

    User->>Chain: **invoke(input)**
    Chain->>Callback: **on_chain_start()**
    Callback->>Handler: **通知开始**

    Chain->>Chain: **执行步骤1**
    Chain->>Callback: **on_llm_start()**
    Callback->>Handler: **通知 LLM 开始**

    Chain->>Chain: **执行步骤2**
    Chain->>Callback: **on_llm_end()**
    Callback->>Handler: **通知 LLM 结束**

    Chain->>Callback: **on_chain_end()**
    Callback->>Handler: **通知结束**
    Chain->>User: **返回结果**
```

## 5. 数据流转

```mermaid
graph LR
    subgraph "**输入处理**"
        Input["**原始输入**"]
        Prompt["**Prompt模板**"]
        FormattedInput["**格式化输入**"]
    end

    subgraph "**模型处理**"
        Model["**LLM/ChatModel**"]
        Processing["**模型推理**"]
        RawOutput["**原始输出**"]
    end

    subgraph "**输出处理**"
        Parser["**OutputParser**"]
        StructuredOutput["**结构化输出**"]
        FinalResult["**最终结果**"]
    end

    Input --> Prompt
    Prompt --> FormattedInput
    FormattedInput --> Model
    Model --> Processing
    Processing --> RawOutput
    RawOutput --> Parser
    Parser --> StructuredOutput
    StructuredOutput --> FinalResult
```

## 6. 扩展机制

LangChain 支持多种扩展方式：

### 6.1 自定义组件

```python
from langchain_core.runnables import RunnableLambda

def custom_function(x):
    return x.upper()

custom_component = RunnableLambda(custom_function)
chain = prompt | model | custom_component
```

### 6.2 自定义工具

```python
from langchain_core.tools import tool

@tool
def my_tool(query: str) -> str:
    """工具描述"""
    return "处理结果"
```

### 6.3 自定义模型

```python
from langchain_core.language_models import BaseChatModel

class MyCustomModel(BaseChatModel):
    def _generate(self, messages, stop=None, **kwargs):
        # 实现模型逻辑
        pass
```

## 7. 与生态系统的集成

```mermaid
graph TB
    subgraph "**LangChain 生态系统**"

        LC["**LangChain**<br/>核心框架"]
        LG["**LangGraph**<br/>Agent编排"]
        LS["**LangSmith**<br/>监控评估"]

        LC <--> LG
        LC <--> LS
        LG <--> LS
    end

    subgraph "**外部集成**"

        Models["**模型提供商**<br/>OpenAI, Anthropic, etc."]
        VectorDB["**向量数据库**<br/>Chroma, Pinecone, etc."]
        Tools["**外部工具**<br/>搜索、API等"]
    end

    LC --> Models
    LC --> VectorDB
    LC --> Tools

    style LC fill:#bbdefb,stroke:#1976d2,stroke-width:3px
    style LG fill:#c8e6c9,stroke:#2e7d32,stroke-width:3px
    style LS fill:#ffe0b2,stroke:#f57c00,stroke-width:3px
```

## 8. 关键特性

### 8.1 同步/异步支持

所有 Runnable 组件自动支持同步和异步调用：

- `invoke()` / `ainvoke()`
- `batch()` / `abatch()`
- `stream()` / `astream()`

### 8.2 流式处理

支持实时流式输出，提升用户体验：

```python
for chunk in chain.stream(input):
    print(chunk, end="", flush=True)
```

### 8.3 批量处理

内置批量处理优化，提高吞吐量：

```python
results = chain.batch([input1, input2, input3])
```

### 8.4 错误处理

支持重试、降级等错误处理策略：

```python
chain_with_retry = chain.with_retry(
    stop_after_attempt=3,
    wait_exponential_jitter=True
)

chain_with_fallback = primary_chain.with_fallbacks([backup_chain])
```

## 9. 下一步

本文档提供了 LangChain 的整体架构概览。更多详细信息请参考：

- [核心模块详解](./02-核心模块详解.md) - 深入了解 langchain-core
- [运行原理与流程](./03-运行原理与流程.md) - 理解执行机制
- [使用场景与案例](./04-使用场景与案例.md) - 实际应用示例

---

**文档版本**: 1.0
**最后更新**: 2025-11-22

